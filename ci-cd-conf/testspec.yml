version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing dependencies for test"
      - npm install
  build:
    commands:
      - echo "Running tests..."
      - npm test || echo "No tests found, skipping"

artifacts:
  files:
    - "**/*"
  discard-paths: no
